
import GenericCRUD from '../../components/GenericCRUD';

export default function ContributeBandPage() {
  return <GenericCRUD collectionName="band" />;
}
  
  export const metadata = {
    title: 'Add Band - Music Scene Archive',
    description: 'Add a new band to the archive.',
  };

